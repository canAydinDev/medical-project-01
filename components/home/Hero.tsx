import HeroCarousel from "./HeroCarousel";
import HeroText from "./HeroText";

function Hero() {
  return (
    <section>
      <HeroText />
      <HeroCarousel />
    </section>
  );
}

export default Hero;
